# Same-origin API: the app calls /api/... which Vite (dev) and nginx (prod) proxy
# to the FastAPI backend on :8010. Leave empty for same-origin. Set an absolute
# URL only if the backend lives on a different host.
VITE_ADMIN_API_URL=
